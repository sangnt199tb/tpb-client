server:
  port: 8081

spring:
  application:
    name: tpb-client
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: tpb-client
            client-secret: ebU9psmCOsbV6cgcehIkV0dKbPa743Gg
            scope: openid, profile, email
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8081/login/oauth2/code/keycloak
        provider:
          keycloak:
            issuer-uri: http://localhost:8080/realms/vneid-realm
            user-name-attribute: preferred_username